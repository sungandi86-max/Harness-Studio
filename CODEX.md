# CODEX

## Working Agreement

- Implement requested behavior directly.
- Keep `localStorage` access inside repositories.
- Keep Project Harness data grouped under `Project` unless it needs independent library behavior.
- Run `npm run lint` and `npm run build` before reporting completion.

## Current Product Focus

Harness Studio is local-first. Supabase, login, AI API calls, and file upload are future work.
